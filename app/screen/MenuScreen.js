import { View, Text, StyleSheet } from 'react-native'; //Componentes do ReactNative


const MenuScreen = () => {
  return(
    <View></View>
  );
};

export default MenuScreen


const styles = StyleSheet.create({});
